---
description: Write HANDOFF.md for this session and suggest starting fresh
---

Write a handoff note for the work in this session, following the `handoff-between-sessions` skill: verified outcomes, the one thing in progress with its exact next step, decisions and why, open questions, useful context (never secrets). Save it as `HANDOFF.md` at the repository root (create or overwrite). Keep it under 40 lines.

Then tell the user, in two lines: the file is written, and that a new session started with "Read HANDOFF.md and continue" will begin at a few thousand tokens instead of the current context size. Do not start any new work in this session.
