---
description: Show what this machine's Claude Code sessions cost and where the headroom is
---

Run `npx -y readyclaw@latest scan --days 7` with the Bash tool and show the user the report as returned, without re-summarising it. Nothing is uploaded; the command reads local transcripts only.

If the report lists findings, end with one sentence naming the single highest-value fix and how to apply it (settings fixes: `npx -y readyclaw@latest headroom --apply`; habits: the relevant skill in this plugin).
